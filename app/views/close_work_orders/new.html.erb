
<nav class="navbar bg-white">
  <div class="container-fluid">
    <a class="navbar-brand" >
      Encerrar ordem de serviço: <strong><%= @work_order.delivery_code %></strong>
    </a>
  </div>
</nav>


<%= form_with(model: [@work_order, @close_work_order]) do |f| %>
  <div class="container-form-modality">
  <div class="form-group col-md-6">
    <%= f.label :delivery_date  %>
    <%= f.date_field :delivery_date %> 
  </div>
  <div class="form-group col-md-6">
    <%= f.label :description %>
    <%= f.text_area :description %> 
  </div>
  <div class="btn-form-modality">        
        <%= f.submit 'Gravar', class: "btn btn-primary" %>
  </div>
  </div>
<% end %> 